# Google Sheets configuration
# Create a Google Cloud project, enable Sheets API, download service account JSON
# Place the JSON file in the project root and reference it here
GOOGLE_SHEET_ID=your_spreadsheet_id_here
GOOGLE_CREDENTIALS_FILE=credentials.json

# Database configuration
# SQLite database will be created automatically at this path
DATABASE_URL=sqlite:///./data/budget.db
